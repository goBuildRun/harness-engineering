# 实施方案

> 主闭环开始前，Lead 须将本文拆解为可映射到 `tasks-dag.md` 的开发明细任务。
> 每个实现任务必须满足 Flow-X 式 7 字段契约：`id / read_files / write_files / action / verify / done`。

## 改动策略

## 开发明细任务

| ID | name | owner | depends_on | read_files | write_files | action | verify | done |
|----|------|-------|------------|------------|-------------|--------|--------|------|
| T1 | 示例任务 | backend-agent | 无 | `src/module/`、`tests/` | `src/module/example.py`、`tests/test_example.py` | 先写失败测试，再完成最小实现 | `<product-test-command>` | AC-1 通过，目标路径通过 active profile 的 `structure_guard` |

> `write_files` 路径须通过：`bash .harness/scripts/structure_guard.sh --path <路径>`  
> 契约须通过：`bash .harness/scripts/task_contract_check.sh --task-dir <harness-workspace/planning/tasks/...>`

## 实施顺序

## 验证方式

## 测试与审查证据

- TEST 报告：`harness-workspace/evidence/test-reports/<task-id>-<Tn>-TEST.md`（tier 要求时）
- REVIEW 报告：`harness-workspace/evidence/review-reports/<task-id>-<Tn>-REVIEW.md`（tier 要求时）
- SUMMARY：`harness-workspace/evidence/summaries/<taskId>-<Tn>-SUMMARY.md`

## Gate 2 结论（进入 TDD 前由 Lead 确认）
