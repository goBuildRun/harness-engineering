---
description: Agent Engineering Lifecycle — 指向产品研发双闭环
alwaysApply: true
---

# Agent Engineering Lifecycle 规则

业务代码写在产品仓库根，架构设计和产品知识归产品仓库所有，Lifecycle runtime 规程在 `harness-engineering/`。

1. 先读 `harness-engineering/AGENTS.md`，再按任务进入 `harness-engineering/docs/getting-started/cli.md` 的对应章节；BMAD、协作和领域文档按需读取，不全量注入。
2. BMAD Planning：产品规格、执行计划和任务包写入产品侧 `harness-workspace/planning/`，通过当前 Planning Gate 后再进入实现；目标统一入口落地后由 `harness start` 机械保证。
3. Lifecycle Execution 必须满足任务身份、范围、风险匹配验证和最终结果四个共同不变式；当前兼容脚本按 planning level、角色和风险选用，独立 QA、完整 evidence、浏览器验证和 GC 不得无条件下放给所有任务。
4. 写前用 `structure_guard --path`，写后用 `structure_guard --diff`；路径须在任务契约中登记，并服从 active product profile，而不是固定产品路径。
5. Lead 不写业务代码；执行 Agent 不得签署被要求的独立 QA 或人工 Gate；产品经验先进入产品 workspace，经人工 review 后再上升为全局规则。

脚本：`harness-engineering/.harness/scripts/`
